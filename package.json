{
  "name": "no-tests-label-bot",
  "version": "1.0.0",
  "description": "A Probot app",
  "author": "Laima Stinskaite <laima.stinskaite@sema4genomics.com> (https://github.com/laisti/no-tests-label-bot.git)",
  "license": "ISC",
  "repository": "https://github.com/laisti/no-tests-label-bot.git",
  "homepage": "https://github.com/laisti/no-tests-label-bot",
  "bugs": "https://github.com/laisti/no-tests-label-bot/issues",
  "keywords": [
    "probot",
    "github",
    "probot-app"
  ],
  "scripts": {
    "dev": "nodemon --exec \"npm start\"",
    "start": "probot run ./index.js",
    "lint": "standard --fix",
    "test": "jest && standard",
    "test:watch": "jest --watch --notify --notifyMode=change --coverage"
  },
  "dependencies": {
    "probot": "^7.0.0"
  },
  "devDependencies": {
    "jest": "^22.4.3",
    "nodemon": "^1.17.2",
    "smee-client": "^1.0.2",
    "standard": "^10.0.3"
  },
  "engines": {
    "node": ">= 8.3.0"
  },
  "standard": {
    "env": [
      "jest"
    ]
  }
}
